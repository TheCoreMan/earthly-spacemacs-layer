;;;###autoload
(defun earthly-mode/sample-function ()
  "If you can see this with M-x, you've set up your local package properly."
  (interactive)
  (message "earthly-mode is present!"))

(provide 'earthly-mode)
